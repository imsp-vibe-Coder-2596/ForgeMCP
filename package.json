{
  "name": "forgemcp",
  "version": "0.1.0",
  "private": true,
  "description": "Enterprise TypeScript framework for building Model Context Protocol (MCP) servers.",
  "license": "MIT",
  "packageManager": "pnpm@11.10.0",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "build": "pnpm -r build",
    "clean": "pnpm -r clean",
    "lint": "pnpm -r lint",
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck",
    "format": "pnpm -r format"
  }
}