/**
 * Represents the lifecycle state of a framework component.
 */
export enum LifecycleState {
  Created = "created",
  Starting = "starting",
  Started = "started",
  Stopping = "stopping",
  Stopped = "stopped"
}